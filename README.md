# Movie Suggester

KMeans-based movie recommendation system in Python.

## How it works
- Uses KMeans clustering to group movies by features
- Suggests movies from the same cluster as the input

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the script: `python movie_suggester.py`

